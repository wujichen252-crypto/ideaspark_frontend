# 英雄区视频资源

将高质感的视频文件与海报图置于本目录：

- `hero.mp4`（或 `hero.webm`）：电影级质感、对比度强、时长建议 ≤ 20s
- `hero.jpg`：视频海报占位图，尺寸建议 1920×1080（或更高），压缩优化

命名可保持一致以便替换；如需修改文件名，请同步更新 `src/views/HomeView.vue` 中的视频与海报引用。

性能建议：
- 优先使用 `webm` 编码，其次 `mp4`
- 控制文件体积（建议 ≤ 8MB），提升首屏加载
- 移动端启用 `playsinline` 与静音 `muted`，保证自动播放策略

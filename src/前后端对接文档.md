# IdeaSpark 前后端对接文档

## 项目概述

IdeaSpark 是一个创新项目管理平台，提供用户管理、团队协作、项目市场、社区交流等功能。本文档详细描述了后端 API 接口，方便前端开发者进行对接。

## 技术栈

- **后端**：Spring Boot 3.4.1 + Java 17 + JPA + MySQL
- **认证**：JWT
- **文件存储**：阿里云 OSS
- **接口风格**：RESTful

## 基础信息

- **API 基础路径**：
  - 生产环境：`http://47.108.232.238:9000/api`
  - 本地开发：`http://localhost:8081/api`
- **认证方式**：Bearer Token（JWT）
- **响应格式**：统一 JSON 格式

### 统一响应格式

```json
{
  "status": 200,
  "message": "操作成功",
  "data": {}
}
```

| 字段    | 类型         | 说明        |
| ------- | ------------ | ----------- |
| status  | number       | HTTP 状态码 |
| message | string       | 响应消息    |
| data    | object/array | 响应数据    |

## 认证机制

所有需要认证的接口都需要在请求头中添加 `Authorization` 字段：

```
Authorization: Bearer {token}
```

## 接口列表

### 1. 用户管理

| 接口         | 方法 | 路径                | 描述                 | 认证 |
| ------------ | ---- | ------------------- | -------------------- | ---- |
| 用户登录     | POST | `/user/login`       | 用户登录并获取 Token | 否   |
| 用户注册     | POST | `/user/register`    | 注册新用户           | 否   |
| 更新用户信息 | POST | `/user/update`      | 更新用户信息         | 是   |
| 查询用户列表 | GET  | `/user/getAllUsers` | 分页查询用户列表     | 是   |
| 删除用户     | POST | `/user/deleteUsers` | 批量删除用户         | 是   |

### 2. 团队管理

| 接口         | 方法   | 路径                                    | 描述                   | 认证 |
| ------------ | ------ | --------------------------------------- | ---------------------- | ---- |
| 获取我的团队 | GET    | `/teams/my`                             | 获取当前用户的团队列表 | 是   |
| 获取团队详情 | GET    | `/teams/{uuid}`                         | 获取团队详细信息       | 是   |
| 更新团队信息 | PUT    | `/teams/{uuid}`                         | 更新团队信息           | 是   |
| 创建协作团队 | POST   | `/teams/collaboration`                  | 创建新的协作团队       | 是   |
| 解散团队     | DELETE | `/teams/{uuid}`                         | 解散团队               | 是   |
| 获取团队成员 | GET    | `/teams/{uuid}/members`                 | 获取团队成员列表       | 是   |
| 更新成员角色 | PUT    | `/teams/{uuid}/members/{memberId}/role` | 更新团队成员角色       | 是   |
| 移除成员     | DELETE | `/teams/{uuid}/members/{memberId}`      | 移除团队成员           | 是   |
| 退出团队     | DELETE | `/teams/{uuid}/members/self`            | 退出团队               | 是   |
| 转让所有权   | POST   | `/teams/{uuid}/transfer-ownership`      | 转让团队所有权         | 是   |
| 发送邀请     | POST   | `/teams/{uuid}/invitations`             | 发送团队邀请           | 是   |

### 3. 项目管理

| 接口         | 方法 | 路径           | 描述                       | 认证 |
| ------------ | ---- | -------------- | -------------------------- | ---- |
| 获取我的项目 | GET  | `/projects/my` | 分页获取当前用户的项目列表 | 是   |

### 4. 项目市场

| 接口         | 方法 | 路径                           | 描述                 | 认证 |
| ------------ | ---- | ------------------------------ | -------------------- | ---- |
| 获取项目列表 | GET  | `/market/projects/list`        | 分页获取项目市场列表 | 否   |
| 获取项目详情 | GET  | `/market/projects/{projectId}` | 获取项目市场详情     | 否   |

### 5. 社区帖子

| 接口         | 方法   | 路径                                 | 描述                         | 认证 |
| ------------ | ------ | ------------------------------------ | ---------------------------- | ---- |
| 创建帖子     | POST   | `/community/posts`                   | 发布新帖子                   | 是   |
| 获取帖子列表 | GET    | `/community/posts`                   | 获取所有帖子                 | 否   |
| 获取帖子详情 | GET    | `/community/posts/{postId}`          | 获取帖子详情（浏览数 +1）    | 否   |
| 更新帖子     | PUT    | `/community/posts/{postId}`          | 更新帖子（仅作者）           | 是   |
| 删除帖子     | DELETE | `/community/posts/{postId}`          | 删除帖子（逻辑删除，仅作者） | 是   |
| 更新点赞数   | PUT    | `/community/posts/{postId}/likes`    | 更新帖子点赞数               | 否   |
| 更新评论数   | PUT    | `/community/posts/{postId}/comments` | 更新帖子评论数               | 否   |

### 6. 社区评论

| 接口         | 方法   | 路径                                     | 描述                           | 认证 |
| ------------ | ------ | ---------------------------------------- | ------------------------------ | ---- |
| 发布评论     | POST   | `/community/comments`                    | 发布评论或回复                 | 是   |
| 获取一级评论 | GET    | `/community/comments/post/{postId}`      | 获取帖子的一级评论             | 否   |
| 获取所有评论 | GET    | `/community/comments/post/{postId}/all`  | 获取帖子的所有评论（包括回复） | 否   |
| 获取回复列表 | GET    | `/community/comments/replies/{parentId}` | 获取评论的所有回复             | 否   |
| 更新评论     | PUT    | `/community/comments/{commentId}`        | 更新评论（仅作者）             | 是   |
| 删除评论     | DELETE | `/community/comments/{commentId}`        | 删除评论（仅作者）             | 是   |

### 7. 社区圈子

| 接口         | 方法   | 路径                                                  | 描述                     | 认证 |
| ------------ | ------ | ----------------------------------------------------- | ------------------------ | ---- |
| 创建圈子     | POST   | `/community/groups`                                   | 创建新圈子               | 是   |
| 获取圈子列表 | GET    | `/community/groups`                                   | 获取所有圈子             | 否   |
| 获取圈子详情 | GET    | `/community/groups/{groupId}`                         | 获取圈子详细信息         | 否   |
| 更新圈子     | PUT    | `/community/groups/{groupId}`                         | 更新圈子（仅创建者）     | 是   |
| 删除圈子     | DELETE | `/community/groups/{groupId}`                         | 删除圈子（仅创建者）     | 是   |
| 加入圈子     | POST   | `/community/groups/{groupId}/join`                    | 加入圈子                 | 是   |
| 退出圈子     | DELETE | `/community/groups/{groupId}/quit`                    | 退出圈子                 | 是   |
| 获取成员列表 | GET    | `/community/groups/{groupId}/members`                 | 获取圈子成员列表         | 否   |
| 移除成员     | DELETE | `/community/groups/{groupId}/members/{memberId}`      | 移除成员（仅管理员）     | 是   |
| 更新成员角色 | PUT    | `/community/groups/{groupId}/members/{memberId}/role` | 更新成员角色（仅管理员） | 是   |

### 8. 社区点赞

| 接口           | 方法   | 路径                                         | 描述           | 认证 |
| -------------- | ------ | -------------------------------------------- | -------------- | ---- |
| 点赞帖子       | POST   | `/community/likes/post/{postId}`             | 给帖子点赞     | 是   |
| 取消点赞帖子   | DELETE | `/community/likes/post/{postId}`             | 取消帖子点赞   | 是   |
| 获取帖子点赞数 | GET    | `/community/likes/post/{postId}/count`       | 获取帖子点赞数 | 否   |
| 检查帖子点赞   | GET    | `/community/likes/post/{postId}/check`       | 检查是否已点赞 | 是   |
| 点赞评论       | POST   | `/community/likes/comment/{commentId}`       | 给评论点赞     | 是   |
| 取消点赞评论   | DELETE | `/community/likes/comment/{commentId}`       | 取消评论点赞   | 是   |
| 获取评论点赞数 | GET    | `/community/likes/comment/{commentId}/count` | 获取评论点赞数 | 否   |
| 检查评论点赞   | GET    | `/community/likes/comment/{commentId}/check` | 检查是否已点赞 | 是   |

### 9. 用户关注

| 接口             | 方法   | 路径                                     | 描述                   | 认证 |
| ---------------- | ------ | ---------------------------------------- | ---------------------- | ---- |
| 关注用户         | POST   | `/follows/{followingId}`                 | 关注指定用户           | 是   |
| 取消关注         | DELETE | `/follows/{followingId}`                 | 取消关注指定用户       | 是   |
| 获取关注列表     | GET    | `/follows/my/following`                  | 获取我的关注列表       | 是   |
| 获取粉丝列表     | GET    | `/follows/my/followers`                  | 获取我的粉丝列表       | 是   |
| 获取关注数       | GET    | `/follows/my/following/count`            | 获取我的关注数         | 是   |
| 获取粉丝数       | GET    | `/follows/my/followers/count`            | 获取我的粉丝数         | 是   |
| 检查关注状态     | GET    | `/follows/check/{followingId}`           | 检查是否已关注         | 是   |
| 获取用户关注列表 | GET    | `/follows/user/{userId}/following`       | 获取指定用户的关注列表 | 否   |
| 获取用户粉丝列表 | GET    | `/follows/user/{userId}/followers`       | 获取指定用户的粉丝列表 | 否   |
| 获取用户关注数   | GET    | `/follows/user/{userId}/following/count` | 获取指定用户的关注数   | 否   |
| 获取用户粉丝数   | GET    | `/follows/user/{userId}/followers/count` | 获取指定用户的粉丝数   | 否   |

### 10. 文件上传

| 接口     | 方法 | 路径           | 描述                 | 认证 |
| -------- | ---- | -------------- | -------------------- | ---- |
| 上传文件 | POST | `/file/upload` | 上传文件到阿里云 OSS | 是   |

### 11. 邀请管理

| 接口     | 方法 | 路径                    | 描述             | 认证 |
| -------- | ---- | ----------------------- | ---------------- | ---- |
| 验证邀请 | GET  | `/invitations/validate` | 验证团队邀请链接 | 是   |

### 12. 系统接口

| 接口       | 方法 | 路径    | 描述           | 认证 |
| ---------- | ---- | ------- | -------------- | ---- |
| 主页       | GET  | `/`     | 系统主页       | 否   |
| 健康检查   | GET  | `/ping` | 系统健康检查   | 否   |
| API 根路径 | GET  | `/api`  | API 根路径说明 | 否   |

## 详细接口文档

### 1. 用户管理

#### 1.1 用户登录

**接口**：`POST /api/user/login`

**请求参数**：

| 字段     | 类型   | 必填 | 描述     |
| -------- | ------ | ---- | -------- |
| email    | string | 是   | 用户邮箱 |
| password | string | 是   | 用户密码 |

**请求示例**：

```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "张三",
      "email": "user@example.com"
    }
  }
}
```

#### 1.2 用户注册

**接口**：`POST /api/user/register`

**请求参数**：

| 字段     | 类型   | 必填 | 描述     |
| -------- | ------ | ---- | -------- |
| username | string | 是   | 用户名   |
| email    | string | 是   | 用户邮箱 |
| password | string | 是   | 用户密码 |

**请求示例**：

```json
{
  "username": "张三",
  "email": "user@example.com",
  "password": "password123"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "张三",
    "email": "user@example.com"
  }
}
```

#### 1.3 更新用户信息

**接口**：`POST /api/user/update`

**请求参数**：

| 字段     | 类型   | 必填 | 描述     |
| -------- | ------ | ---- | -------- |
| username | string | 否   | 用户名   |
| email    | string | 否   | 用户邮箱 |
| password | string | 否   | 用户密码 |

**请求示例**：

```json
{
  "username": "李四",
  "email": "newemail@example.com"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "username": "李四",
    "email": "newemail@example.com"
  }
}
```

#### 1.4 查询用户列表

**接口**：`GET /api/user/getAllUsers`

**请求参数**：

| 字段     | 类型   | 必填 | 描述                |
| -------- | ------ | ---- | ------------------- |
| page     | number | 否   | 页码（默认 1）      |
| size     | number | 否   | 每页大小（默认 10） |
| username | string | 否   | 用户名搜索          |
| email    | string | 否   | 邮箱搜索            |

**请求示例**：

```
GET /api/user/getAllUsers?page=1&size=10&username=张
```

**响应示例**：

```json
{
  "status": 200,
  "message": "查询成功",
  "data": {
    "content": [
      {
        "id": 1,
        "username": "张三",
        "email": "user@example.com"
      }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "size": 10,
    "number": 0
  }
}
```

#### 1.5 删除用户

**接口**：`POST /api/user/deleteUsers`

**请求参数**：

| 字段    | 类型  | 必填 | 描述         |
| ------- | ----- | ---- | ------------ |
| userIds | array | 是   | 用户 ID 数组 |

**请求示例**：

```json
{
  "userIds": [1, 2, 3]
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "删除成功"
}
```

### 2. 团队管理

#### 2.1 获取我的团队

**接口**：`GET /api/teams/my`

**请求参数**：

| 字段 | 类型   | 必填 | 描述                |
| ---- | ------ | ---- | ------------------- |
| page | number | 否   | 页码（默认 1）      |
| size | number | 否   | 每页大小（默认 10） |

**请求示例**：

```
GET /api/teams/my?page=1&size=10
```

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "teams": [
      {
        "uuid": "team-123",
        "name": "开发团队",
        "description": "核心开发团队",
        "memberCount": 5,
        "role": "OWNER"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  }
}
```

#### 2.2 获取团队详情

**接口**：`GET /api/teams/{uuid}`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "uuid": "team-123",
    "name": "开发团队",
    "description": "核心开发团队",
    "ownerId": 1,
    "memberCount": 5,
    "createdAt": "2024-01-01T00:00:00",
    "role": "OWNER"
  }
}
```

#### 2.3 更新团队信息

**接口**：`PUT /api/teams/{uuid}`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**请求参数**：

| 字段        | 类型   | 必填 | 描述     |
| ----------- | ------ | ---- | -------- |
| name        | string | 否   | 团队名称 |
| description | string | 否   | 团队描述 |

**请求示例**：

```json
{
  "name": "前端开发团队",
  "description": "负责前端开发的团队"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "团队信息更新成功",
  "data": {
    "uuid": "team-123",
    "name": "前端开发团队",
    "description": "负责前端开发的团队"
  }
}
```

#### 2.4 创建协作团队

**接口**：`POST /api/teams/collaboration`

**请求参数**：

| 字段        | 类型   | 必填 | 描述     |
| ----------- | ------ | ---- | -------- |
| name        | string | 是   | 团队名称 |
| description | string | 否   | 团队描述 |

**请求示例**：

```json
{
  "name": "新产品开发团队",
  "description": "负责新产品的开发"
}
```

**响应示例**：

```json
{
  "status": 201,
  "message": "协作团队创建成功",
  "data": {
    "uuid": "team-456",
    "name": "新产品开发团队",
    "description": "负责新产品的开发",
    "ownerId": 1
  }
}
```

#### 2.5 解散团队

**接口**：`DELETE /api/teams/{uuid}`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**响应示例**：

```json
{
  "status": 200,
  "message": "团队解散成功",
  "data": {
    "uuid": "team-123"
  }
}
```

#### 2.6 获取团队成员

**接口**：`GET /api/teams/{uuid}/members`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**请求参数**：

| 字段 | 类型   | 必填 | 描述                |
| ---- | ------ | ---- | ------------------- |
| page | number | 否   | 页码（默认 1）      |
| size | number | 否   | 每页大小（默认 10） |

**请求示例**：

```
GET /api/teams/team-123/members?page=1&size=10
```

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "members": [
      {
        "id": 1,
        "username": "张三",
        "email": "user@example.com",
        "role": "OWNER",
        "joinedAt": "2024-01-01T00:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  }
}
```

#### 2.7 更新成员角色

**接口**：`PUT /api/teams/{uuid}/members/{memberId}/role`

**路径参数**：

| 字段     | 类型   | 描述      |
| -------- | ------ | --------- |
| uuid     | string | 团队 UUID |
| memberId | string | 成员 ID   |

**请求参数**：

| 字段 | 类型   | 必填 | 描述                           |
| ---- | ------ | ---- | ------------------------------ |
| role | string | 是   | 新角色（OWNER, ADMIN, MEMBER） |

**请求示例**：

```json
{
  "role": "ADMIN"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "成员角色修改成功",
  "data": {
    "memberId": "1",
    "role": "ADMIN"
  }
}
```

#### 2.8 移除成员

**接口**：`DELETE /api/teams/{uuid}/members/{memberId}`

**路径参数**：

| 字段     | 类型   | 描述      |
| -------- | ------ | --------- |
| uuid     | string | 团队 UUID |
| memberId | string | 成员 ID   |

**响应示例**：

```json
{
  "status": 200,
  "message": "成员移除成功",
  "data": {
    "memberId": "2"
  }
}
```

#### 2.9 退出团队

**接口**：`DELETE /api/teams/{uuid}/members/self`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**响应示例**：

```json
{
  "status": 200,
  "message": "成功退出团队",
  "data": {
    "uuid": "team-123"
  }
}
```

#### 2.10 转让所有权

**接口**：`POST /api/teams/{uuid}/transfer-ownership`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**请求参数**：

| 字段       | 类型   | 必填 | 描述        |
| ---------- | ------ | ---- | ----------- |
| newOwnerId | number | 是   | 新所有者 ID |

**请求示例**：

```json
{
  "newOwnerId": 2
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "团队所有权转让成功",
  "data": {
    "uuid": "team-123",
    "newOwnerId": 2
  }
}
```

#### 2.11 发送邀请

**接口**：`POST /api/teams/{uuid}/invitations`

**路径参数**：

| 字段 | 类型   | 描述      |
| ---- | ------ | --------- |
| uuid | string | 团队 UUID |

**请求参数**：

| 字段   | 类型   | 必填 | 描述                      |
| ------ | ------ | ---- | ------------------------- |
| emails | array  | 是   | 邀请邮箱数组              |
| role   | string | 是   | 邀请角色（ADMIN, MEMBER） |

**请求示例**：

```json
{
  "emails": ["user1@example.com", "user2@example.com"],
  "role": "MEMBER"
}
```

**响应示例**：

```json
{
  "status": 201,
  "message": "邀请发送成功",
  "data": {
    "uuid": "team-123",
    "invitedCount": 2
  }
}
```

### 3. 项目管理

#### 3.1 获取我的项目

**接口**：`GET /api/projects/my`

**请求参数**：

| 字段    | 类型   | 必填 | 描述                |
| ------- | ------ | ---- | ------------------- |
| page    | number | 否   | 页码（默认 1）      |
| size    | number | 否   | 每页大小（默认 10） |
| keyword | string | 否   | 搜索关键词          |
| status  | string | 否   | 项目状态筛选        |

**请求示例**：

```
GET /api/projects/my?page=1&size=10&keyword=创新
```

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "projects": [
      {
        "id": "project-123",
        "name": "创新项目",
        "description": "创新创业项目",
        "status": "ACTIVE",
        "memberCount": 3,
        "createdAt": "2024-01-01T00:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  }
}
```

### 4. 项目市场

#### 4.1 获取项目列表

**接口**：`GET /api/market/projects/list`

**请求参数**：

| 字段     | 类型   | 必填 | 描述                |
| -------- | ------ | ---- | ------------------- |
| page     | number | 否   | 页码（默认 1）      |
| size     | number | 否   | 每页大小（默认 10） |
| keyword  | string | 否   | 搜索关键词          |
| category | string | 否   | 项目分类            |

**请求示例**：

```
GET /api/market/projects/list?page=1&size=10&keyword=AI
```

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "projects": [
      {
        "id": "project-123",
        "name": "AI 助手",
        "description": "智能 AI 助手项目",
        "category": "AI",
        "likeCount": 100,
        "viewCount": 1000
      }
    ],
    "total": 1,
    "page": 1,
    "size": 10
  }
}
```

#### 4.2 获取项目详情

**接口**：`GET /api/market/projects/{projectId}`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| projectId | string | 项目 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "id": "project-123",
    "name": "AI 助手",
    "description": "智能 AI 助手项目",
    "category": "AI",
    "likeCount": 100,
    "viewCount": 1000,
    "createdAt": "2024-01-01T00:00:00",
    "updatedAt": "2024-01-02T00:00:00",
    "author": {
      "id": 1,
      "username": "张三"
    }
  }
}
```

### 5. 社区帖子

#### 5.1 创建帖子

**接口**：`POST /api/community/posts`

**请求参数**：

| 字段       | 类型   | 必填 | 描述                                  |
| ---------- | ------ | ---- | ------------------------------------- |
| title      | string | 是   | 帖子标题                              |
| content    | string | 是   | 帖子内容                              |
| images     | string | 否   | 图片 URL（多张图片用逗号分隔）        |
| tags       | string | 否   | 标签（多个标签用逗号分隔）            |
| channel    | string | 否   | 频道分类                              |
| visibility | string | 否   | 可见性（PUBLIC/PRIVATE，默认 PUBLIC） |
| projectId  | string | 否   | 关联的项目 ID                         |

**请求示例**：

```json
{
  "title": "我的创新项目分享",
  "content": "大家好，今天分享一下我的创新项目...",
  "images": "https://example.com/img1.jpg,https://example.com/img2.jpg",
  "tags": "创新，创业，技术",
  "channel": "技术分享",
  "projectId": "project-123"
}
```

**响应示例**：

```json
{
  "status": 201,
  "message": "created",
  "data": {
    "id": "post-123",
    "title": "我的创新项目分享",
    "content": "大家好，今天分享一下我的创新项目...",
    "createdAt": "2024-01-01T00:00:00"
  }
}
```

#### 5.2 获取帖子列表

**接口**：`GET /api/community/posts`

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "post-123",
      "title": "我的创新项目分享",
      "content": "大家好，今天分享一下我的创新项目...",
      "author": {
        "id": 1,
        "username": "张三"
      },
      "viewsCount": 100,
      "likesCount": 20,
      "commentsCount": 5,
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 5.3 获取帖子详情

**接口**：`GET /api/community/posts/{postId}`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "id": "post-123",
    "title": "我的创新项目分享",
    "content": "大家好，今天分享一下我的创新项目...",
    "images": "https://example.com/img1.jpg,https://example.com/img2.jpg",
    "tags": "创新，创业，技术",
    "channel": "技术分享",
    "visibility": "PUBLIC",
    "viewsCount": 101,
    "likesCount": 20,
    "commentsCount": 5,
    "author": {
      "id": 1,
      "username": "张三"
    },
    "project": {
      "id": "project-123",
      "name": "创新项目"
    },
    "createdAt": "2024-01-01T00:00:00",
    "updatedAt": "2024-01-01T00:00:00"
  }
}
```

#### 5.4 更新帖子

**接口**：`PUT /api/community/posts/{postId}`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**请求参数**：

| 字段       | 类型   | 必填 | 描述     |
| ---------- | ------ | ---- | -------- |
| title      | string | 否   | 帖子标题 |
| content    | string | 否   | 帖子内容 |
| images     | string | 否   | 图片 URL |
| tags       | string | 否   | 标签     |
| visibility | string | 否   | 可见性   |

**请求示例**：

```json
{
  "title": "更新后的标题",
  "content": "更新后的内容..."
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "更新成功",
  "data": {
    "id": "post-123",
    "title": "更新后的标题",
    "content": "更新后的内容..."
  }
}
```

#### 5.5 删除帖子

**接口**：`DELETE /api/community/posts/{postId}`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "帖子删除成功（逻辑删除）"
}
```

### 6. 社区评论

#### 6.1 发布评论

**接口**：`POST /api/community/comments`

**请求参数**：

| 字段     | 类型   | 必填 | 描述                    |
| -------- | ------ | ---- | ----------------------- |
| postId   | string | 是   | 帖子 ID                 |
| content  | string | 是   | 评论内容                |
| parentId | string | 否   | 父评论 ID（回复时填写） |

**请求示例**：

```json
{
  "postId": "post-123",
  "content": "写得很好，学习了！"
}
```

**回复示例**：

```json
{
  "postId": "post-123",
  "content": "感谢支持！",
  "parentId": "comment-456"
}
```

**响应示例**：

```json
{
  "status": 201,
  "message": "评论发布成功",
  "data": {
    "id": "comment-789",
    "content": "写得很好，学习了！",
    "user": {
      "id": 2,
      "username": "李四"
    },
    "post": {
      "id": "post-123",
      "title": "我的创新项目分享"
    },
    "createdAt": "2024-01-01T00:00:00"
  }
}
```

#### 6.2 获取帖子的一级评论

**接口**：`GET /api/community/comments/post/{postId}`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "comment-123",
      "content": "写得很好，学习了！",
      "user": {
        "id": 2,
        "username": "李四"
      },
      "likesCount": 5,
      "createdAt": "2024-01-01T00:00:00",
      "replies": []
    }
  ]
}
```

#### 6.3 获取帖子的所有评论

**接口**：`GET /api/community/comments/post/{postId}/all`

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "comment-123",
      "content": "写得很好，学习了！",
      "user": {
        "id": 2,
        "username": "李四"
      },
      "parentId": null,
      "likesCount": 5,
      "createdAt": "2024-01-01T00:00:00"
    },
    {
      "id": "comment-456",
      "content": "感谢支持！",
      "user": {
        "id": 1,
        "username": "张三"
      },
      "parentId": "comment-123",
      "likesCount": 2,
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 6.4 获取评论的回复

**接口**：`GET /api/community/comments/replies/{parentId}`

**路径参数**：

| 字段     | 类型   | 描述      |
| -------- | ------ | --------- |
| parentId | string | 父评论 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "comment-456",
      "content": "感谢支持！",
      "user": {
        "id": 1,
        "username": "张三"
      },
      "likesCount": 2,
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 6.5 更新评论

**接口**：`PUT /api/community/comments/{commentId}`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| commentId | string | 评论 ID |

**请求参数**：

| 字段    | 类型   | 必填 | 描述     |
| ------- | ------ | ---- | -------- |
| content | string | 是   | 评论内容 |

**请求示例**：

```json
{
  "content": "更新后的评论内容"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "更新成功",
  "data": {
    "id": "comment-123",
    "content": "更新后的评论内容"
  }
}
```

#### 6.6 删除评论

**接口**：`DELETE /api/community/comments/{commentId}`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| commentId | string | 评论 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "评论删除成功"
}
```

### 7. 社区圈子

#### 7.1 创建圈子

**接口**：`POST /api/community/groups`

**请求参数**：

| 字段        | 类型   | 必填 | 描述       |
| ----------- | ------ | ---- | ---------- |
| name        | string | 是   | 圈子名称   |
| keyword     | string | 否   | 圈子关键词 |
| description | string | 否   | 圈子描述   |
| iconUrl     | string | 否   | 图标 URL   |
| coverUrl    | string | 否   | 封面图 URL |

**请求示例**：

```json
{
  "name": "创新创业交流群",
  "keyword": "创新创业",
  "description": "专注于创新创业项目的交流群",
  "iconUrl": "https://example.com/icon.jpg",
  "coverUrl": "https://example.com/cover.jpg"
}
```

**响应示例**：

```json
{
  "status": 201,
  "message": "圈子创建成功",
  "data": {
    "id": "group-123",
    "name": "创新创业交流群",
    "keyword": "创新创业",
    "description": "专注于创新创业项目的交流群",
    "createdBy": {
      "id": 1,
      "username": "张三"
    },
    "createdAt": "2024-01-01T00:00:00"
  }
}
```

#### 7.2 获取圈子列表

**接口**：`GET /api/community/groups`

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "group-123",
      "name": "创新创业交流群",
      "keyword": "创新创业",
      "description": "专注于创新创业项目的交流群",
      "memberCount": 50,
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 7.3 获取圈子详情

**接口**：`GET /api/community/groups/{groupId}`

**路径参数**：

| 字段    | 类型   | 描述    |
| ------- | ------ | ------- |
| groupId | string | 圈子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "id": "group-123",
    "name": "创新创业交流群",
    "keyword": "创新创业",
    "description": "专注于创新创业项目的交流群",
    "iconUrl": "https://example.com/icon.jpg",
    "coverUrl": "https://example.com/cover.jpg",
    "createdBy": {
      "id": 1,
      "username": "张三"
    },
    "memberCount": 50,
    "createdAt": "2024-01-01T00:00:00"
  }
}
```

#### 7.4 更新圈子

**接口**：`PUT /api/community/groups/{groupId}`

**路径参数**：

| 字段    | 类型   | 描述    |
| ------- | ------ | ------- |
| groupId | string | 圈子 ID |

**请求参数**：

| 字段        | 类型   | 必填 | 描述       |
| ----------- | ------ | ---- | ---------- |
| name        | string | 否   | 圈子名称   |
| keyword     | string | 否   | 圈子关键词 |
| description | string | 否   | 圈子描述   |
| iconUrl     | string | 否   | 图标 URL   |
| coverUrl    | string | 否   | 封面图 URL |

**请求示例**：

```json
{
  "name": "更新后的圈子名称",
  "description": "更新后的描述"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "更新成功",
  "data": {
    "id": "group-123",
    "name": "更新后的圈子名称",
    "description": "更新后的描述"
  }
}
```

#### 7.5 删除圈子

**接口**：`DELETE /api/community/groups/{groupId}`

**路径参数**：

| 字段    | 类型   | 描述    |
| ------- | ------ | ------- |
| groupId | string | 圈子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "圈子删除成功"
}
```

#### 7.6 加入圈子

**接口**：`POST /api/community/groups/{groupId}/join`

**路径参数**：

| 字段    | 类型   | 描述    |
| ------- | ------ | ------- |
| groupId | string | 圈子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "加入圈子成功"
}
```

#### 7.7 退出圈子

**接口**：`DELETE /api/community/groups/{groupId}/quit`

**路径参数**：

| 字段    | 类型   | 描述    |
| ------- | ------ | ------- |
| groupId | string | 圈子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "退出圈子成功"
}
```

#### 7.8 获取圈子成员列表

**接口**：`GET /api/community/groups/{groupId}/members`

**路径参数**：

| 字段    | 类型   | 描述    |
| ------- | ------ | ------- |
| groupId | string | 圈子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "member-123",
      "user": {
        "id": 1,
        "username": "张三"
      },
      "role": "admin",
      "joinedAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 7.9 移除成员

**接口**：`DELETE /api/community/groups/{groupId}/members/{memberId}`

**路径参数**：

| 字段     | 类型   | 描述    |
| -------- | ------ | ------- |
| groupId  | string | 圈子 ID |
| memberId | string | 成员 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "成员移除成功"
}
```

#### 7.10 更新成员角色

**接口**：`PUT /api/community/groups/{groupId}/members/{memberId}/role`

**路径参数**：

| 字段     | 类型   | 描述    |
| -------- | ------ | ------- |
| groupId  | string | 圈子 ID |
| memberId | string | 成员 ID |

**请求参数**：

| 字段 | 类型   | 必填 | 描述                 |
| ---- | ------ | ---- | -------------------- |
| role | string | 是   | 角色（admin/member） |

**请求示例**：

```json
{
  "role": "admin"
}
```

**响应示例**：

```json
{
  "status": 200,
  "message": "角色更新成功",
  "data": {
    "memberId": "member-123",
    "role": "admin"
  }
}
```

### 8. 社区点赞

#### 8.1 点赞帖子

**接口**：`POST /api/community/likes/post/{postId}`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "帖子点赞成功"
}
```

#### 8.2 取消点赞帖子

**接口**：`DELETE /api/community/likes/post/{postId}`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "取消帖子点赞成功"
}
```

#### 8.3 获取帖子点赞数

**接口**：`GET /api/community/likes/post/{postId}/count`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "count": 20
  }
}
```

#### 8.4 检查是否点赞帖子

**接口**：`GET /api/community/likes/post/{postId}/check`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| postId | string | 帖子 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "liked": true
  }
}
```

#### 8.5 点赞评论

**接口**：`POST /api/community/likes/comment/{commentId}`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| commentId | string | 评论 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "评论点赞成功"
}
```

#### 8.6 取消点赞评论

**接口**：`DELETE /api/community/likes/comment/{commentId}`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| commentId | string | 评论 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "取消评论点赞成功"
}
```

#### 8.7 获取评论点赞数

**接口**：`GET /api/community/likes/comment/{commentId}/count`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| commentId | string | 评论 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "count": 5
  }
}
```

#### 8.8 检查是否点赞评论

**接口**：`GET /api/community/likes/comment/{commentId}/check`

**路径参数**：

| 字段      | 类型   | 描述    |
| --------- | ------ | ------- |
| commentId | string | 评论 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "liked": true
  }
}
```

### 9. 用户关注

#### 9.1 关注用户

**接口**：`POST /api/follows/{followingId}`

**路径参数**：

| 字段        | 类型   | 描述          |
| ----------- | ------ | ------------- |
| followingId | number | 被关注用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "关注用户成功"
}
```

#### 9.2 取消关注

**接口**：`DELETE /api/follows/{followingId}`

**路径参数**：

| 字段        | 类型   | 描述                |
| ----------- | ------ | ------------------- |
| followingId | number | 被取消关注的用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "取消关注用户成功"
}
```

#### 9.3 获取我的关注列表

**接口**：`GET /api/follows/my/following`

**响应示例**：

```json
{
  "status": 200,
  "message": "success",
  "data": [
    {
      "id": "follow-123",
      "followingId": 2,
      "followingName": "李四",
      "followingAvatar": "https://example.com/avatar.jpg",
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 9.4 获取我的粉丝列表

**接口**：`GET /api/follows/my/followers`

**响应示例**：

```json
{
  "status": 200,
  "message": "success",
  "data": [
    {
      "id": "follow-456",
      "followerId": 3,
      "followerName": "王五",
      "followerAvatar": "https://example.com/avatar.jpg",
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 9.5 获取我的关注数

**接口**：`GET /api/follows/my/following/count`

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "count": 10
  }
}
```

#### 9.6 获取我的粉丝数

**接口**：`GET /api/follows/my/followers/count`

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "count": 20
  }
}
```

#### 9.7 检查是否已关注

**接口**：`GET /api/follows/check/{followingId}`

**路径参数**：

| 字段        | 类型   | 描述            |
| ----------- | ------ | --------------- |
| followingId | number | 被检查的用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "following": true
  }
}
```

#### 9.8 获取用户的关注列表

**接口**：`GET /api/follows/user/{userId}/following`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| userId | number | 用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "success",
  "data": [
    {
      "id": "follow-123",
      "followingId": 2,
      "followingName": "李四",
      "followingAvatar": "https://example.com/avatar.jpg",
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 9.9 获取用户的粉丝列表

**接口**：`GET /api/follows/user/{userId}/followers`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| userId | number | 用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "success",
  "data": [
    {
      "id": "follow-456",
      "followerId": 3,
      "followerName": "王五",
      "followerAvatar": "https://example.com/avatar.jpg",
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

#### 9.10 获取用户的关注数

**接口**：`GET /api/follows/user/{userId}/following/count`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| userId | number | 用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "count": 10
  }
}
```

#### 9.11 获取用户的粉丝数

**接口**：`GET /api/follows/user/{userId}/followers/count`

**路径参数**：

| 字段   | 类型   | 描述    |
| ------ | ------ | ------- |
| userId | number | 用户 ID |

**响应示例**：

```json
{
  "status": 200,
  "message": "获取成功",
  "data": {
    "count": 20
  }
}
```

### 10. 文件上传

#### 10.1 上传文件

**接口**：`POST /api/file/upload`

**请求参数**：

| 字段 | 类型 | 必填 | 描述       |
| ---- | ---- | ---- | ---------- |
| file | file | 是   | 上传的文件 |

**请求示例**：

```
POST /api/file/upload
Content-Type: multipart/form-data

[表单数据]
file: [文件内容]
```

**响应示例**：

```json
{
  "status": 200,
  "message": "上传成功",
  "data": {
    "url": "https://oss-cn-beijing.aliyuncs.com/ideaspark/uploads/file123.jpg",
    "filename": "file123.jpg",
    "size": 102400
  }
}
```

### 11. 邀请管理

#### 11.1 验证邀请

**接口**：`GET /api/invitations/validate`

**请求参数**：

| 字段  | 类型   | 必填 | 描述     |
| ----- | ------ | ---- | -------- |
| token | string | 是   | 邀请令牌 |

**请求示例**：

```
GET /api/invitations/validate?token=invite-token-123
```

**响应示例**：

```json
{
  "status": 200,
  "message": "邀请验证成功",
  "data": {
    "valid": true,
    "team": {
      "uuid": "team-123",
      "name": "开发团队"
    },
    "role": "MEMBER"
  }
}
```

### 12. 系统接口

#### 12.1 主页

**接口**：`GET /`

**响应示例**：

```json
{
  "status": 200,
  "message": "IdeaSpark 后端服务已启动",
  "data": {
    "version": "0.0.1-SNAPSHOT",
    "docs": "/actuator/health, /api/user/*"
  }
}
```

#### 12.2 健康检查

**接口**：`GET /ping`

**响应示例**：

```json
{
  "status": 200,
  "message": "pong",
  "data": "pong"
}
```

#### 12.3 API 根路径

**接口**：`GET /api`

**响应示例**：

```json
{
  "status": 200,
  "message": "API 根路径",
  "data": {
    "user": "/api/user/login, /api/user/register, /api/user/getAllUsers, /api/user/deleteUsers"
  }
}
```

## 错误处理

### 常见错误码

| 状态码 | 描述           | 示例消息             |
| ------ | -------------- | -------------------- |
| 400    | 请求参数错误   | "请求参数格式错误"   |
| 401    | 未授权         | "请先登录"           |
| 403    | 禁止访问       | "没有权限执行此操作" |
| 404    | 资源不存在     | "用户不存在"         |
| 500    | 服务器内部错误 | "服务器内部错误"     |

### 错误响应示例

```json
{
  "status": 401,
  "message": "请先登录",
  "data": null
}
```

## 开发环境配置

### 本地开发

1. 克隆项目
2. 配置 `application.yml` 中的数据库连接
3. 运行 `IdeaSparkApplication.java` 启动服务
4. 访问 `http://localhost:8081/ping` 验证服务是否启动

### 环境变量配置

在项目根目录创建 `.env` 文件：

```env
# 数据库配置
DB_URL=jdbc:mysql://localhost:3306/ideaspark?useSSL=false&serverTimezone=Asia/Shanghai
DB_USERNAME=root
DB_PASSWORD=your_password

# JWT 配置
JWT_SECRET=your_jwt_secret

# 阿里云 OSS 配置
OSS_ACCESS_KEY_ID=your_oss_key_id
OSS_ACCESS_KEY_SECRET=your_oss_secret
```

## 部署说明

1. 打包项目：`mvn clean package -DskipTests`
2. 运行 JAR 文件：`java -jar target/ideaspark-0.0.1-SNAPSHOT.jar`
3. 或使用 Docker 部署

## 注意事项

1. **认证**：所有需要认证的接口都需要在请求头中添加 `Authorization: Bearer {token}`
2. **文件上传**：文件大小限制取决于服务器配置
3. **请求频率**：请合理控制 API 请求频率，避免过度请求
4. **数据安全**：敏感操作请确保用户身份验证和权限检查

## 联系方式

如有问题，请联系后端开发团队。

---

**文档版本**：2.1.0
**更新时间**：2026-03-22
**更新内容**：

- 修复本地开发端口为 8081
- 更新用户关注模块响应格式（简化字段，避免循环引用）
- 更新创建帖子响应格式（简化返回数据）
- 新增本地开发环境 API 基础路径说明
